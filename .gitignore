node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
.vite/

# Sampled instrument banks: tens of megabytes, fetched on demand with
# `npm run sounds:fetch`. The app falls back to its synthesised instruments
# when they are absent, so a clone without them still plays.
apps/web/public/sounds/

# Test corpora: user-contributed transcriptions of copyrighted songs.
# Fetched on demand with `npm run fixtures:fetch`. Never committed.
fixtures/

# Editor / vault config
.obsidian/
.idea/
.vscode/

# Screenshot output from `run shot`
screenshot.png
test-results/

# Playwright MCP session output
.playwright-mcp/

# vite-plugin-pwa dev output
dev-dist/
